name: Build Signed APK
on:
  push:
    tags:
      - '*'       # 上游同步新TAG自动触发打包（全自动）
  workflow_dispatch: # 手动只能选main启动脚本

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # 拉取全部标签，用来筛选最新TAG

      - name: 1. 自动切换源码
        run: |
          # TAG推送自动触发：使用当前TAG源码
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            TAG=${GITHUB_REF#refs/tags/}
            echo "APP_VER=${TAG#*-}" >> $GITHUB_ENV
          else
            # 手动main启动：筛选编号最大最新TAG，切换到对应源码
            LATEST_TAG=$(git tag | sort -t'-' -k1 -n | tail -1)
            if [ -n "$LATEST_TAG" ]; then
              git checkout $LATEST_TAG
              echo "APP_VER=${LATEST_TAG#*-}" >> $GITHUB_ENV
            else
              echo "APP_VER=dev" >> $GITHUB_ENV
            fi
          fi

      - name: 预配置Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: temurin

      - name: 2. 动态注入个人签名与架构精简配置（Kotlin DSL 专配版）
        run: |
          # 针对原生的 app/build.gradle.kts 末尾追加严格符合 Kotlin 规范的定制代码
          cat << 'EOF' >> app/build.gradle.kts

          android {
              defaultConfig {
                  ndk {
                      // 强制清空其余架构，只保留 arm64-v8a 精简体积
                      abiFilters.clear()
                      abiFilters.add("arm64-v8a")
                  }
              }
              
              if (System.getenv("KEYSTORE_PASSWORD") != null) {
                  signingConfigs {
                      maybeCreate("customRelease").apply {
                          storeFile = file("mykey.jks")
                          storePassword = System.getenv("KEYSTORE_PASSWORD")
                          keyAlias = System.getenv("KEY_ALIAS")
                          keyPassword = System.getenv("KEY_PASSWORD")
                      }
                  }
                  buildTypes {
                      getByName("release") {
                          // 明确关闭混淆与资源缩减，保障插件或模块绝对稳健运行
                          isMinifyEnabled = false
                          isShrinkResources = false
                          signingConfig = signingConfigs.getByName("customRelease")
                      }
                  }
              } else {
                  buildTypes {
                      getByName("release") {
                          isMinifyEnabled = false
                          isShrinkResources = false
                      }
                  }
              }
          }
          EOF

      - name: 3. Decode Keystore
        env:
          KEYSTORE: ${{ secrets.KEYSTORE }}
        run: echo -n "$KEYSTORE" | base64 -d > app/mykey.jks

      - name: 4. Make gradlew executable
        run: chmod +x gradlew

      - name: 5. Build Release APK
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: ./gradlew assembleRelease

      - name: 6. Rename APK(only arm64-v8a)
        env:
          VER: ${{ env.APP_VER }}
        run: |
          # 动态匹配生成的 release 目录下的 apk
          SRC_APK=$(find app/build/outputs/apk/release -name "*.apk" | head -1)
          mv "$SRC_APK" "./Sesame-AG-arm64-v8a-${VER}-signed.apk"

      - name: 7. Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Signed-APK
          path: Sesame-AG-arm64-v8a-${{ env.APP_VER }}-signed.apk
